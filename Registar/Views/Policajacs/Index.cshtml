@model IEnumerable<Registar.Models.Policajac>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PolicajacIme)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PolicajacPrezime)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PolicajacIme)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PolicajacPrezime)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PolicajacID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PolicajacID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PolicajacID })
        </td>
    </tr>
}

</table>
